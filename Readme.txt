Hello, Hope you are doing good.

First open this directory in VS Code.

Then Open the terminal and run this command "npm i" to download node Modules.

Then run this command "npm run dev" to start the server.